import { Meta } from '@storybook/addon-docs/blocks';
import { ChartConfigDropdown } from '../../Elements/ChartConfigDropdown';

<Meta title="Dashboard/Data types/graphDataConfiguration" />

### graphDataConfiguration

Type - `Array<object>`

Defines which data sheet column maps to each graph parameter. This object depends on the graph type. Select a chart type below to see example object.

__Please note: This is NOT applicable for `geoHubMap`,`geoHubMapWithLayerSelection`, and `geoHubCompareMap`.

<ChartConfigDropdown type='dataSettings'/>
